default:
  engine: anthropic
  model: claude-sonnet-4-6

roles: {}

maxConcurrent: 4
retryBudget: 3
keepEnvironmentForReview: false
executionMode: local  # 'local' skips sandbox deployment, 'sandbox' requires K8s
