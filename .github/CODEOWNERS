# Workflow and Actions files require explicit approval from repo owner
/.github/workflows/    @scain
/.github/actions/      @scain

# Protect GitHub config-as-code files
/.github/CODEOWNERS    @scain

# Protect the deploy script — it runs against the tailnet host
/scripts/deploy.sh     @scain
