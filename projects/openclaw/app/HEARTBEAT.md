# OpenClaw Heartbeat

Phase 1 placeholder. This file will hold the safety-net monitor logic (stalled plans, PR/CI checks, memory pruning) once cron is enabled in later phases.

For now: no cron runs. No periodic work happens. The gateway is idle unless a human is chatting with it.
