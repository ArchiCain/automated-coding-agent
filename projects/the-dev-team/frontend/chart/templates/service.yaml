apiVersion: v1
kind: Service
metadata:
  name: {{ include "the-dev-team-frontend.fullname" . }}
  labels:
    {{- include "the-dev-team-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "the-dev-team-frontend.selectorLabels" . | nindent 4 }}
