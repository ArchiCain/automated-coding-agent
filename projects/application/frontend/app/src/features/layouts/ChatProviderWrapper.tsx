import { ReactNode } from 'react';

/**
 * Placeholder wrapper - chat provider removed during migration.
 * Retained to avoid breaking layout imports.
 */
export function ChatProviderWrapper({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
